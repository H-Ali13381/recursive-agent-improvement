# Tests placeholder

Suggested future checks:

- config parses
- dataset split has no leakage
- one tiny batch can run forward/backward
- metrics match known examples
- export bundle contains required files
- tool wrapper returns valid JSON for a fixture input
- invalid input returns structured error JSON
